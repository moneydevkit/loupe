/target
Cargo.lock.bak

# Local planning doc, kept out of git.
/LOUPE.md

# Local config & state
.env
.env.*
!.env.example

# Cert / key material — never check in
*.pem
*.key
*.crt
*.p12
*.pfx

# SQLite databases used during development / smoke tests
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# direnv
.direnv/

# Nix build
result

# local VM root disk (deploy/README.md)
loupe.qcow2

# generated findings reports (just report / loupe-report) — may carry
# sensitive findings from private repos; never commit
loupe-*.html

# scratch commit message and host-local sandbox binds
commit-message.txt
.isolate
